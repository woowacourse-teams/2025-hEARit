<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="item"
            type="com.onair.hearit.domain.model.RecommendHearit" />

        <variable
            name="recommendClickListener"
            type="com.onair.hearit.presentation.home.HearitClickListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:background="@drawable/bg_gray1_radius_8dp"
        android:onClick="@{() -> recommendClickListener.onClick(item.id)}">

        <TextView
            android:id="@+id/tv_recommend_hearit_category"
            style="@style/pretendard_main_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="12dp"
            android:text="@{item.categoryName}"
            android:textSize="24sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Android" />

        <ImageView
            android:id="@+id/iv_recommend_hearit_disk"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:contentDescription="@null"
            android:scaleType="fitCenter"
            android:src="@drawable/img_home_record"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_recommend_hearit_category" />

        <TextView
            android:id="@+id/tv_recommend_hearit_title"
            style="@style/pretendard_category"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginVertical="24dp"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="16dp"
            android:text="@{item.title}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/iv_recommend_hearit_disk"
            tools:text="스프링 @EventListener와 @TransactionalEventListener" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
